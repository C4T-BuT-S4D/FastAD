version: v2
lint:
  use:
  - STANDARD
  except:
  - FIELD_NOT_REQUIRED
  - PACKAGE_NO_IMPORT_CYCLE
  - PACKAGE_VERSION_SUFFIX
  disallow_comment_ignores: true
breaking:
  use:
    - WIRE_JSON
  except:
  - FIELD_SAME_DEFAULT
